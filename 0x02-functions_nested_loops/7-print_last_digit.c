#include "main.h"
/**
 * print_last_digit - prints the last digit.
 * @n: return the value of last digit
 * Return: always 0
 */
int print_last_digit(int n)
{

	print_last_digit(98);
	print_last_digit(0);
	n = print_last_digit(-1024);
	_putchar('0' + n);
	_putchar('\n');
	return (0);
}
